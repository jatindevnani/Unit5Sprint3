package _Q2_Book_Reading_System_LLD;

public class Main {
	
	 /* Listing down the requirements
	  * 
	 * ----Book reader system----
	 * 
	 * - User can be a member or not a member
	 * - All users can search for books
	 * - Only Members can read
	 * - Membership required to be a member
	 * - Login and logout required
	 * - Only one active user at a time
	 * - That user can only read one book at a time
	 * - Administrator can add and remove books
	 * 
	 * 
	 * Actors
	 * 
	 * - System i.e. The book reader class
	 * - Users --> extends --> Registered
	 * - Admin --> Only one admin can exist
	 */
	
	
}		
